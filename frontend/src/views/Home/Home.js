import React from 'react';
import './Home.css';
import { UploadForm } from '../../components/UploadForm';

const Home = props => {
  console.log(props);
  return (
      <UploadForm />
  );
};

export default Home;
